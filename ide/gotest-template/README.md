# How to use on VSCODE

Copy this directory and put anywhere you like e.g. `/path/to/template_dir`, then add `go.generateTestsFlags` to the settings.json.

```json
    "go.generateTestsFlags": ["-template_dir", "/path/to/template_dir"]
```
